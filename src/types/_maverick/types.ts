export type Agent = {
  id: number;
};

export type Campaign = {
  id: number;
};

export type Company = {
  name: string | null;
};

export type Dba = {
  name: string | null;
};

export type Signature = {
  status: string;
};